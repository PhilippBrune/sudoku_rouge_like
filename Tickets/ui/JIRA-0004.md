id: JIRA-0004
status: DONE
summary: Menu music smoothing, path layout polish, puzzle item usage, shop modal, HUD details, navigation, and reward/shop icon UX
description:
- Make Main Menu sound longer and smoother loop.
- Garden Path: avoid tile overlap and route both lanes to one shared boss gate tile in middle-bottom area.
- Show items with icon + name and keep dynamic hover description area.
- Implement item usage during puzzle.
- Hide Solve button.
- Add shop modal window with select-one-or-none and replacement choice when inventory cap is reached.
- In puzzle HUD show garden level/current level context, difficulty, and stars.
- Use complementary highlight colors for row/column/number emphasis.
- Add grid navigation via Arrow keys and WASD.
- Reward screen: show icon + name and dynamic hover description box.
attachments:
- User screenshot: empty garden path overview.
- User request in chat (March 5, 2026).

implementation_notes:
- Menu loop extended and seam-smoothed in `MenuMusicController`.
- Added puzzle item bar (icon + name + hover details) with in-grid item usage.
- Added shop modal with choose-one-or-none flow and replacement selection when inventory is full.
- Added reward cards with icon + name and hover description panel.
- Added shared middle-bottom boss gate rendering and lane overlap mitigation in garden path view.
- Added puzzle HUD context (depth, run level, puzzle size, difficulty, stars).
