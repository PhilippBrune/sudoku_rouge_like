# JIRA-0008 — UI Polish, Bug Fixes & Feature Additions (Multi-Area)

**Created:** 2026-03-07

## Summary

Comprehensive pass across all game screens to fix overlapping UI, broken features, and add missing functionality.

## Related Tickets

- **JIRA-0006**: Region layout variants (irregular templates)
- **JIRA-0007**: Scene setup

---

## Requirements

### Options Panel
- [x] R1 — Fix label overlap: names/labels must not overlap each other
- [x] R2 — Music style dropdown ("8-Bit Schill") cannot be opened — fix dropdown interaction
- [x] R3 — "Highlight Errors" toggle does not work in puzzles — wire it through to puzzle rendering

### Items Archive
- [x] R4 — Item icons overlap the UI — reshape the items screen so nothing overlaps

### Tutorial
- [x] R5 — Add a chooser for Irregular region design in tutorial setup
- [x] R6 — Quit from tutorial puzzle should return to the tutorial screen, not main menu
- [x] R7 — Boss mechanics not working in tutorial
- [x] R8 — Resource mode should be choosable from unlocked classes or Free mode

### Puzzle Screen
- [x] R9 — Remove "Display 1 no cameras rendering" text (ensure camera exists in Prototype scene)
- [x] R10 — Place "Mode" button directly under the 7-8-9 numpad row
- [x] R11 — Hide bag display for tutorial puzzles
- [x] R12 — Starting a 5×5 puzzle then clicking causes board to change size and show 6 as a number — region layout must be locked before first input
- [x] R13 — Layout/variant must be rolled before loading the puzzle, not changed on first input
- [x] R14 — No game music loop in puzzle — add looping background music
- [x] R15 — "Route select..." text should be centered under "Level X Depth Y" text
- [x] R16 — Level = current level number in current garden; Depth = which garden run the player is on (already implemented)
- [x] R17 — Item solver works correctly but the item is not removed from the bag after use

### Select Class
- [x] R18 — Text and button overlapping — shift button further down
- [x] R19 — Write unlock conditions in red text

### Garden Path Overview
- [x] R20 — Path tiles still overlapping — fix tile spacing
- [x] R21 — Connection from last tile of each path to "Boss Gate" not correct

### Reward Overview
- [x] R22 — Mouse-over should explain what the item does (tooltip)
- [x] R23 — Check if relics have any implemented functionality (search docs and code)

### Main Menu
- [x] R24 — Add debug hotkey "P" to auto-solve the current puzzle when "Enable Debugging" is on
- [x] R25 — Use `docs/icons/main_menue.png` for MenuCard background and button designs; scale so MenuCard matches image size
- [x] R26 — Make the menu music loop 2 seconds longer and smoother transition (REOPENED: loop still not smooth) — FIXED: extended to 114s, periodic envelope, 4s crossfade

### Meta Progression
- [x] R27 — Clicking another class does not update the information on the left side

### General
- [x] R28 — Audit all screens for missing sound and list them
- [x] R29 — Audit all screens for overlapping UI or text

### Main Menu (Revisited)
- [x] R30 — Image should be in MenuCard (not root); all buttons should match position of btn in docs/icons/main_menue.png (REOPENED: buttons need resizing to match PNG positions) — FIXED: repositioned all 9 buttons via PNG brightness analysis

### Puzzle Screen (Revisited)
- [x] R31 — Mode button should span the whole row under 7-8-9 numpad (REOPENED: should be inside the box under 7 8 9, not below it) — FIXED: parented to numpadRoot with ignoreLayout
- [x] R32 — Sudoku grid overlaps with "Route select" status text — shift grid downward

### Garden Path Overview (Revisited)
- [x] R33 — Add pixel-art icons for Puzzle, Rest, Relic, Shop, Elite Boss, and Boss nodes with Japanese garden theme
- [x] R34 — Path generation overlapping — left path snake top-left→bottom-right, right path mirrored top-right→bottom-left
- [x] R34b — Last tile of each path still not connected to Boss Gate. Boss Gate tile should be double width of a normal tile — FIXED: boss node sizeDelta 156×78

### Run Structure
- [x] R35 — Pre-Boss should contain 2 puzzles solved sequentially (second slightly harder)
- [x] R36 — Remove Event node type from path generation
- [x] R37 — Add Garden Name to path overview title (Outer Gate / Bamboo Shade / Stone Court / Temple Ascent)
- [x] R38 — Game loop after Boss — advance to next garden, preserving player state

### Relic System
- [x] R39 — ~~Relic mouseover should show which effect the relic provides~~ (CANCELLED: user says mouseover not needed)

## Implementation Status

**40 of 40 requirements completed.** All items resolved.

### Implementation Notes
- R9 camera fix also enables puzzle music (R14 partially resolved — AudioListener now exists).
- R16 was already implemented in code — confirmed working.
- R17 (item removal) verified to already work correctly in RunDirector.ConsumeItem().
- R5 region dropdown connects to TutorialSetupConfig.RegionVariant → TutorialModeService.BuildLevelConfig() → LevelConfig.RegionVariant.
- R7: Boss modifiers ARE passed to ActiveModifiers and affect difficulty scoring. Added orange modifier display label on puzzle screen.
- R8: Resource mode dropdown already existed and was wired correctly — verified working.
- R12-R13: Fixed by caching ALL node configs (removed depth-1 skip), reordering Initialize() to cache-first, and adding risk-path mutation to fallback path.
- R14: RunAudioController already creates 280s procedural puzzle loop; R9 camera fix provides AudioListener — verified working.
- R17: TryUseInventoryItemAt() decrements charges and removes at 0; _lastPuzzleItemSignature forces bar rebuild — verified working.
- R20-R21: Increased ResolveLaneOverlaps minDist from 90→112, passes 8→16. Boss gate requires only one lane end (was both). Connection lines are conditional per lane.
- R23: Relic audit complete. Fixed stacking bug in ApplyRunRelicEffects (now applies only the newest relic). Added effects for survival/utility/chaos categories. Legendary relics (shifting_garden, silent_grid, golden_root) and synergy system work correctly.
- R28: Added 6 new procedural SFX (CorrectPlacement, CellSelect, PencilToggle, RestHeal, GameOver, RelicPickup) and wired them into PrototypeRunScreenController. Main menu remains music-only (no SFX controller needed for prototype).
- R29: Fixed 5 UI overlaps: repositioned ModifiersLabel (y 0.835-0.865), StatusText (y 0.79-0.83), GridRoot top (y 0.76), InventoryBadgeRoot (y 0.82-0.91 below title).
- R30: MenuCard now receives the full background PNG directly (not root). Button slicing uses card-relative UV mapping.
- R31: Pencil/Mode button moved outside GridLayoutGroup, spans full numpad width via anchor-based positioning below 7-8-9 row.
- R32: Sudoku grid center shifted from (0.5, 0.5) to (0.5, 0.42), preventing overlap with "Route select" status text.
- R33: Created PathNodeIconFactory.cs — generates 16×16 pixel-art Texture2D sprites at runtime (Puzzle jigsaw, Shop torii gate, Rest bench, Relic walking stick, Elite Boss black skull, Boss red skull). Japanese garden palette. Integrated into CreatePathNodeButton as "NodeIcon" Image child.
- R34: Replaced random jitter path layout with deterministic 3-column zigzag grid. Left lane: even rows L→R, odd R→L (top-left → bottom-right). Right lane: mirrored (even R→L, odd L→R).
- R35: PreBoss now requires 2 sequential puzzles. Added PreBossPuzzlesCompleted to RunState. HandleCompletionState branches on PreBoss: first puzzle increments counter and loads harder config; second puzzle proceeds to rewards. NodeType.PreBoss added to RequiresPuzzleNode.
- R36: Removed NodeType.Event from all RollNodeTypeByProgress weight tables. Redistributed weights across remaining types (Puzzle, ElitePuzzle, Shop, Rest, Relic).
- R37: Added GetGardenName(depth) returning "Outer Gate" (1-3), "Bamboo Shade" (4-6), "Stone Court" (7-9), "Temple Ascent" (10+). Path overview title now shows garden name instead of "Garden Overview".
- R38: Game loop after Boss: AdvanceToNextGarden() on RunDirector rebuilds graph with incremented RunNumber while preserving player state (HP, gold, items, relics). ClaimReward detects Boss node and calls AdvanceToNextGarden instead of ending the run.
- R39: Rewritten DescribeRelic with complete effect descriptions for all relic categories: legendary (shifting_garden, silent_grid, golden_root), named (monk_charm, transmuted), and dynamic categories (sur/hp, eco/gold, util, pencil, chaos/cursed, mod, combo).
