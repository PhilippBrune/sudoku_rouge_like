# JIRA-0015 — Repo Cleanup: .gitignore, Backup & Push

**Created:** 2026-03-08

## Summary

Clean up the repository so only game-relevant files are tracked in git.
Move unused tooling/scripting artifacts to a local backup folder.
Establish a standing workflow: always review existing tickets before each prompt.

## Requirements

### Git / .gitignore
- [x] R1 — Update `.gitignore` so only `Assets/`, Unity config folders (`Packages/`, `ProjectSettings/`), `docs/`, and `Tickets/` (plus `README.md`, `.github/`, `.githooks/`) are tracked. Everything else is excluded.

### Cleanup — move to `C:\Users\Philipp\Documents\backup\sudoku_rouge_like\`
- [x] R2 — `foocus_api/` — Fooocus image-generation scripts and logs
- [x] R3 — `retired/` — archived legacy script copies
- [x] R4 — `ai/` — local AI agent tooling
- [x] R5 — `tools/` — empty local tools dir
- [x] R6 — `requirements/` — empty requirements dir
- [x] R7 — `_scan_png.py`, `_scan_png2.py`, `_scan_result.txt`, `_scan_result2.txt` — one-off scan utilities
- [x] R8 — `agent.ps1` — local PowerShell runner
- [x] R9 — `_empty/` — empty placeholder dir
- [x] R10 — `.venv/` gitignored (593 MB, not moved — easily recreated)
- [x] R11 — `.vscode/` gitignored (IDE settings)

### Commit & Push
- [x] R12 — Stage all JIRA-0014 code fixes and the gitignore/cleanup changes, commit, and push to `main`.

### Standing Workflow
- [x] R13 — For every future prompt: review existing tickets first, reopen if the request overlaps a previously completed ticket, create a new ticket if it does not.

## Implementation Status

- [x] R1 — .gitignore updated
- [x] R2–R9 — Files moved to backup
- [x] R10–R11 — .venv and .vscode gitignored
- [x] R12 — Committed and pushed
- [x] R13 — Workflow saved to memory
